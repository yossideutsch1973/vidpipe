# Test pattern generation and processing
# Generate test pattern, process it, and display
test-pattern with (width: 800, height: 600, pattern: "checkerboard") -> grayscale -> edges with (low_threshold: 100, high_threshold: 200) -> display