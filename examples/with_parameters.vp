# Pipeline with function parameters
# Capture from specific camera, apply blur, then display in named window
webcam with (camera_id: 1) -> blur with (kernel_size: 15, sigma: 5.0) -> display with (window_name: "Blurred Video")